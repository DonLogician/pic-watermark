# 配置常量

# 支持的图片格式
SUPPORTED_FORMATS = ['.jpg', '.jpeg', '.png']

# 默认字体大小
DEFAULT_FONT_SIZE = 24

# 默认水印位置
DEFAULT_WATERMARK_POSITION = (10, 10)

# 默认水印颜色（白色）
DEFAULT_WATERMARK_COLOR = (255, 255, 255, 255)